# Issue 002: Promote Smoke Boundary Into IfcModelReader

> *This was generated by AI during triage.*

## Triage

- Category: `enhancement`
- State: `ready-for-agent`
- Type: `AFK`
- Blocked by: Issue 001

## Context To Read

- `CONTEXT.md`
- `context/domain.md`
- `UBIQUITOUS_LANGUAGE.md`
- `context/prds/milestone-1-ifc-evidence-extractor.md`
- `context/specs/module-architecture.md`
- `context/specs/ifc-evidence-extractor.md`
- `context/decisions/2026-06-05-ifc-parser-architecture.md`

## What To Build

Promote the small smoke-test parser boundary into the real `IfcModelReader` seam and build the first targeted `IfcExtractionIndex`. The CLI should still run end-to-end, but now through reader/index interfaces instead of raw `web-ifc` calls.

This issue proves official relationship paths can be accessed and indexed without exposing raw parser mechanics to domain modules.

## Acceptance Criteria

- [ ] `IfcModelReader` interface exists with the methods defined in the extractor spec.
- [ ] `WebIfcModelReader` hides `web-ifc` lifecycle, model id, entity constants, line id collection shapes, and attribute wrapper quirks.
- [ ] CLI uses `IfcModelReader` instead of raw `web-ifc`.
- [ ] Relevant element discovery rules live in static config.
- [ ] `IfcExtractionIndex` indexes links touching relevant elements and their type objects.
- [ ] Index includes `IfcRelDefinesByType`.
- [ ] Index includes `IfcTypeObject.HasPropertySets`.
- [ ] Index includes `IfcRelAssociatesMaterial`.
- [ ] Index includes `IfcRelDefinesByProperties`.
- [ ] `domain` code does not import raw `web-ifc`.
- [ ] Tests or smoke checks prove the CLI still works against the private IFC.

## User Stories Covered

6, 7, 8, 9, 10, 18, 19, 20, 21

