# General Component Topology Kernel Issue 002 — Define the Recipe and Primitive Registry Contract

## Question

What is the smallest versioned Declarative Construction Recipe and Primitive Registry interface that can compose layers, rectangular/C/Z/hat profiles, one or two parallel aligned or staggered rows, spacing, cavities, breaks, and boundaries without family-specific kernel branching, while allowing later primitives and dimension-specific Topology Modules to register without changing the kernel?

## Triage

- label: wayfinder:grilling
- state: ready-for-agent
- AFK/HITL: HITL

## Blocked by

None.

## Resolution must decide

- The domain ownership of derived IFC values, architect-confirmed values, validated constants, estimates, and solver controls.
- The recipe's invariants, coordinate system, units, representative-cell semantics, material-region ownership, and boundary vocabulary.
- How conditional fields and primitive-specific parameters remain declarative without turning the common interface into a union of every possible family field.
- How primitive capability, recipe schema, topology module, pack, and worker versions declare compatibility.
- What a future primitive may extend and what requires a new Topology Module instead.

