# Decide CPU↔GPU reconstruction, critical-path, and semantic adapters on emulator evidence

Status: open
Type: grilling
Blocked by: 01, 03

## Question

What is the canonical request-scoped dependency DAG over shared execution (nodes, edges, correlation IDs, clock alignment with uncertainty, shared batch nodes), the differential critical-path localization rule under concurrency, the suspect-subgraph extraction policy, and the framework-adapter semantic enrichment design — all against emulator/fake evidence — with lagged correlation kept as baseline and dynamic roofline as classification only?
