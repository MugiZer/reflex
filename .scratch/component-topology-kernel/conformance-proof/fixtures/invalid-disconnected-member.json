{
  "expect": {
    "category": "invalid",
    "outcome": "rejected",
    "reason": "Primitive 'fixture.disconnected' emitted invalid local geometry."
  },
  "recipe": {
    "fixtureBase": "valid-timber-framing.json",
    "patch": {
      "rows[0].member.primitive.kind": "fixture.disconnected",
      "rows[0].member.primitive.version": "1.0.0",
      "rows[0].member.primitive.parameters": {
        "width": 0.045,
        "depth": 0.14
      }
    }
  }
}
