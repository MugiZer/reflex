# 02 — Apply the selected Review mode to every Requested Input

**What to build:** Apply the selected Review mode to all Requested Inputs. Library mode pre-populates every credible Material Library match and shows the selected entry/value; manual mode starts blank; mix mode pre-populates matches and leaves unresolved inputs for manual entry. Users can change individual inputs while preserving accurate datapoint provenance.

**Blocked by:** 01 — Add the Review setup choice before the Architect Action View.

**Status:** ready-for-agent

- [ ] Library mode uses only server-backed Material Library entries for pre-populated values.
- [ ] Manual mode leaves values blank and marks entered values as User Input.
- [ ] Mix mode combines matched library values with manual completion for unresolved inputs.
- [ ] Per-input changes update the displayed source and submitted provenance correctly.